# CLAUDE.md — 외국인 일자리 적법성 판정 서비스

이 파일은 Claude Code가 이 프로젝트에서 작업할 때의 **불변 규칙**을 정의한다.
기능 구현 전에 반드시 읽고, 아래 INVARIANT를 위반하는 코드는 작성하지 않는다.

---

## 1. 이 제품이 무엇인가

외국인(D-2 / D-4 / E-9 / E-7-4)이 채용공고를 볼 때
**"내 비자로 여기서 일하면 합법인가?"** 에 근거와 함께 답하는 판정 엔진과,
그 판정을 필터로 쓰는 일자리 검색 서비스.

**핵심 자산은 공고 데이터가 아니라 `visa_rules` 테이블과 판정 엔진이다.**
공고는 복제 가능하지만 판정 근거 데이터셋은 복제되지 않는다.

**MVP 범위:** 수도권 대학가(서울 동대문구 회기·이문·휘경) D-2·D-4 유학생 시간제취업.
E-9 / E-7-4는 스키마·엔진 레벨에서만 지원하고 **UI에는 노출하지 않는다.**

상세 배경은 `기획서_v0.2.md`, 제품 요구사항은 `spec/PRD.md` 참조.

---

## 2. INVARIANT — 절대 위반 금지

이 항목들은 기능 요구가 아니라 **법적 제약**이다. "나중에 추가하자"도 안 된다.
관련 코드를 작성하라는 요청을 받으면, 구현하지 말고 이 파일을 근거로 거부하고 사용자에게 확인할 것.

### L1. 알선(職業紹介)을 구현하지 않는다
우리는 **직업정보제공사업**(직업안정법 제23조, 신고제)으로 운영한다.
유료직업소개사업(제19조, 등록제)으로 넘어가면 법인 자본금 5천만 원·직업상담사 자격 임원 2명·독립 사무실 요건이 발생한다.

**리포지토리에 존재해서는 안 되는 것:**
- ❌ 지원서/이력서 제출·전달 기능
- ❌ 구직자–사업주 간 1:1 메시징·채팅
- ❌ 면접 일정 조율 기능
- ❌ 채용 성사 기반 수수료 로직, 성사 추적(placement tracking)
- ❌ 구직자에 대한 과금 코드 (결제 모듈에 job_seeker 경로 자체를 만들지 않는다)

**허용되는 것:** 검색, 필터, 판정, 원문 링크아웃, 알림, 사업주의 공고 직접 등록.

> E-9 사업장 변경은 고용센터 알선이 원칙이다. E-9 관련 화면에는
> "사업장 변경은 고용센터를 통해서만 가능합니다. 이 서비스는 사전 정보 제공 도구입니다."를 **항상** 표시한다.

### L2. 비자 규칙을 하드코딩하지 않는다
출입국 지침은 자주 바뀐다(유학생 시간제취업 지침만 해도 2026-07-09 개정).

- ❌ `if (visa === 'D2' && hours > 30)` 같은 코드
- ✅ `visa_rules` 테이블 조회 → 규칙 평가

모든 규칙은 `effective_from` / `effective_to`로 버전 관리한다.
과거 판정은 **그 시점의 규칙 버전으로 재현 가능해야 한다**
(`eligibility_evaluations.rule_snapshot`).

### L3. 판정은 반드시 근거와 함께 출력한다
근거 없는 판정을 반환하는 코드 경로를 만들지 않는다.

모든 판정 응답은 다음 3종을 포함한다:
1. `reasons[]` — 적용된 규칙과 판단 내용
2. `rule_effective_date` — 근거 지침의 시행/개정일
3. `disclaimer` — "최종 판단은 관할 출입국·외국인청의 심사에 따릅니다"

### L4. 애매하면 UNKNOWN, 절대 ELIGIBLE 아니다
판정 기본값은 `UNKNOWN`이다.

```
규칙이 없음        → UNKNOWN
규칙 confidence=low → UNKNOWN (또는 CONDITIONAL + 경고)
공고 필드 결측     → UNKNOWN
```

**틀린 `ELIGIBLE` 하나가 사용자에게 강제퇴거를, 사업주에게 3천만 원 벌금을 안긴다.**
정확도보다 보수성이 우선이다.

### L5. 식별번호를 수집·저장하지 않는다
- ❌ 외국인등록번호, 여권번호, 주민등록번호
- ✅ 비자 **종류**, 학위과정, 한국어 능력 급수, 학교

DB 컬럼으로도 만들지 않는다. 폼 필드로도 만들지 않는다.

### L6. 원문(raw)은 자산이 아니라 부채다
크롤링 민사 노출을 줄이는 유일한 구조적 수단.

- 원문 본문(`raw_postings.body`)은 **TTL 7일 후 NULL 처리**한다 (배치 필수 구현)
- 영구 보관은 `url`, `content_hash`, **정규화된 필드만**
- 원문 전문을 사용자에게 **노출하지 않는다.** 상세는 항상 원문 링크아웃
- 공고 카드에는 요약·구조화 필드만 표시

### L7. 크롤러는 접근제한을 우회하지 않는다
대법원 2021도1533에서 형사 무죄를 가른 기준을 그대로 코드 제약으로 삼는다.

- ❌ 로그인 세션·쿠키를 사용한 수집
- ❌ `robots.txt` Disallow 경로 수집
- ❌ 크롤링 금지 명시 문구가 있는 사이트 수집
- ❌ 봇 차단 우회 (CAPTCHA 우회, 헤더 위장으로 차단 회피)
- ✅ 비회원이 접근 가능한 공개 페이지만
- ✅ `User-Agent`에 서비스명과 연락처 이메일 명시
- ✅ 소스당 동시 요청 1, 요청 간 최소 3초

**모든 크롤 소스는 `sources` 테이블의 row다.** 코드에 하드코딩하지 않는다.
`sources.enabled = false` 한 줄로 즉시 중단 가능해야 한다 (킬스위치).

### L8. 개인정보는 저장 전에 제거한다
커뮤니티·게시판 공고 본문에는 전화번호·카톡ID·이메일·작성자명이 그대로 있다.

정규화 파이프라인 **첫 단계**에서 마스킹한다. LLM API로 전송하기 **전에** 수행한다
(국외이전 대상 축소).

---

## 3. 기술 스택

```
Frontend/API   Next.js 15 (App Router) + TypeScript + Tailwind
DB             PostgreSQL 16 (Supabase 또는 Neon)
ORM            Prisma
크롤러/워커     별도 Node 프로세스 (Playwright + cheerio), Postgres 기반 잡 큐
LLM            Anthropic Claude API (structured output / tool use로 JSON 강제)
i18n           next-intl — ko / en / zh-CN / vi
지도·경로       카카오 로컬 API (좌표), ODsay 또는 카카오 길찾기 (대중교통 소요시간)
알림           웹푸시 (web-push) + 이메일 (Resend)
배포           Vercel (web) + Railway/Fly.io (worker) + Supabase (DB)
```

**의도적으로 제외:** Redis, Kubernetes, 마이크로서비스, pgvector.
MVP 규모(공고 수천 건)에서 Postgres FTS + 태그 필터로 충분하다. 인프라를 늘리지 않는다.

---

## 4. 디렉터리 구조

```
/
├── CLAUDE.md
├── 기획서_v0.2.md
├── spec/
│   ├── PRD.md                 제품 요구사항·화면·수용 기준
│   ├── DB_SCHEMA.sql          Postgres DDL (정본)
│   ├── API_SPEC.md            엔드포인트 명세
│   └── VISA_RULES.md          비자 규칙 데이터 모델 + 시드 + 출처·신뢰도
├── apps/
│   ├── web/                   Next.js
│   └── worker/                크롤러 + 정규화 + 판정 배치
└── packages/
    ├── db/                    Prisma schema, migrations, seed
    ├── eligibility/           ⚡ 판정 엔진 (순수 함수, DB 의존 없음)
    └── shared/                타입, i18n 키, 상수
```

**`packages/eligibility`는 순수 함수로 유지한다.**
입력: `(posting: PostingFacts, profile: VisaProfile, rules: VisaRule[])`
출력: `EligibilityResult`
DB·네트워크·환경변수에 의존하지 않는다. → 테스트 가능성이 이 제품의 생명이다.

---

## 5. 개발 순서

법적으로 가장 위험한 부분을 **가장 먼저, 가장 단단하게** 만든다.

| # | 단계 | 산출물 | 완료 기준 |
|---|---|---|---|
| 1 | DB 스키마 + Prisma | `packages/db` | 마이그레이션 통과, seed 실행 |
| 2 | **판정 엔진** | `packages/eligibility` | **골든 테스트 100케이스 통과** (아래 6장) |
| 3 | 비자 규칙 시드 | `VISA_RULES.md` → seed | confidence 플래그 포함, 출처 URL 전부 기입 |
| 4 | 공고 CRUD + 검색 API | `apps/web/api` | 필터 조합 동작 |
| 5 | 사업주 직접 등록 폼 | web | 등록 3분 내 완료 가능 |
| 6 | 구직자 온보딩 + 프로필 | web | 텍스트 입력 0회로 완료 가능 |
| 7 | 검색 결과 + 판정 배지 UI | web | 근거 3종 세트 항상 표시 |
| 8 | i18n (ko/en/zh/vi) | web | 판정 근거 문구까지 번역 |
| 9 | 크롤러 (B급 소스부터) | worker | 킬스위치 동작 확인 |
| 10 | LLM 정규화 파이프라인 | worker | 개인정보 마스킹이 LLM 호출 **전에** 수행됨 |
| 11 | 원문 TTL 파기 배치 | worker | 7일 경과분 body NULL 확인 |
| 12 | 알림 | web+worker | |

> **2번을 건너뛰고 UI부터 만들지 않는다.** 판정 엔진이 틀리면 나머지는 전부 무의미하다.

---

## 6. 판정 엔진 테스트 규칙

`packages/eligibility/__tests__/golden/` 에 케이스를 YAML로 관리한다.

```yaml
- id: D2-MFG-001
  description: D-2 학사 3학년, TOPIK 3급, 제조업 공고 → 불가 (TOPIK 4급 미만)
  profile: { visa: D2, degree: BACHELOR_3_4, korean: TOPIK3 }
  posting: { ksic: "C29", weekly_hours: 20, commute_minutes: 25, employment_form: DIRECT }
  expect:
    status: INELIGIBLE
    reason_codes: [INDUSTRY_DENIED]
```

**규칙:**
- 모든 규칙 타입마다 최소 3케이스 (가능 / 불가 / 경계)
- `UNKNOWN` 케이스를 반드시 포함한다 (규칙 결측, 필드 결측)
- **규칙 데이터를 변경하면 골든 테스트를 먼저 갱신하고 검수자를 기록한다**
- 정확도 95% 미만이면 배포 금지

---

## 7. 코딩 컨벤션

- 판정 결과 타입은 `EligibilityStatus` union — boolean으로 축약하지 않는다
  (`isEligible: boolean` 금지. UNKNOWN과 INELIGIBLE이 구분되어야 한다)
- 시간 단위는 항상 **주당 시간(weekly hours)** 으로 정규화해 저장
- 금액은 항상 **시급(KRW/hour)** 으로 정규화. 월급 공고는 변환 시 `is_estimated=true`
- 좌표는 WGS84 (lat, lng). 통학 시간은 `commute_cache`에 캐싱 (API 비용)
- 모든 사용자 노출 문자열은 i18n 키 경유. 판정 근거 문구도 예외 없음
- 마이그레이션은 `DB_SCHEMA.sql`을 정본으로 하고 Prisma schema를 맞춘다

---

## 8. 요청받아도 만들지 말 것 (체크리스트)

작업 지시가 아래에 해당하면 **구현 전에 사용자에게 확인**한다.

- [ ] 지원하기 / 이력서 보내기 / 채팅 → **L1 위반**
- [ ] 로그인이 필요한 사이트 크롤링 → **L7 위반**
- [ ] 원문 전문을 상세 페이지에 표시 → **L6 위반**
- [ ] 외국인등록번호·여권번호 입력 필드 → **L5 위반**
- [ ] 판정 로직에 비자 조건 if문 추가 → **L2 위반**
- [ ] 규칙 없을 때 기본값을 ELIGIBLE로 → **L4 위반**
- [ ] 구직자 결제 → **L1 위반**

---

## 9. 현재 미해결 (코드에 반영 전 확인 필요)

`기획서_v0.2.md` 부록 A 참조. 아래 값들은 **2차 출처 기반이며 출입국 지침 원문 미대조 상태**다.
시드 데이터에 `confidence: 'low'`로 넣고, 판정 시 `UNKNOWN` 또는 경고를 동반한다.

- D-2 학위과정별 주당 허용시간 (25 vs 30시간 자료 상충)
- **통학 거리 제한 (60분 vs 수도권 90분 — 자료 상충. 핵심 필터라 검증 최우선)**
- 제조업 TOPIK 4급 예외의 심사 재량 범위
- 방학·주말 상한 미적용 조건
- 2026-07-09 개정 세부 내용

**이 값들이 검증되기 전에는 관련 판정을 `CONDITIONAL` 이상으로 올리지 않는다.**
